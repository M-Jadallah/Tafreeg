import {useState} from 'react';import {NavLink,Outlet,useNavigate} from 'react-router-dom';import {Activity,FileText,Gauge,Logs,Menu,Plus,Settings,Users,X,LogOut} from 'lucide-react';import {post} from '../api';
const links=[['/','لوحة التحكم',Gauge],['/new','تفريغ جديد',Plus],['/jobs','التفريغات',FileText],['/workers','العمال',Users],['/logs','السجلات',Logs],['/settings','الإعدادات',Settings]] as const;
export default function Layout(){const[open,setOpen]=useState(false);const nav=useNavigate();async function logout(){await post('/auth/logout');nav('/login')}return <div className="shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><Activity/><div><b>مفرغ يوتيوب</b><small>Deepgram Whisper</small></div><button className="icon mobile" onClick={()=>setOpen(false)}><X/></button></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><button className="logout" onClick={logout}><LogOut size={18}/>تسجيل الخروج</button></aside>{open&&<div className="overlay" onClick={()=>setOpen(false)}/>}<main><header><button className="icon mobile" onClick={()=>setOpen(true)}><Menu/></button><div><b>نظام التفريغ الخاص</b><small>خمسة عمال مستقلين</small></div></header><div className="content"><Outlet/></div></main></div>}
