import {FormEvent,useEffect,useState} from 'react';import {useNavigate} from 'react-router-dom';import {api,post} from '../api';
export default function Login(){const[u,setU]=useState('');const[p,setP]=useState('');const[e,setE]=useState('');const[busy,setBusy]=useState(false);const nav=useNavigate();useEffect(()=>{api('/auth/me').then(()=>nav('/')).catch(()=>{})},[nav]);async function submit(x:FormEvent){x.preventDefault();setBusy(true);setE('');try{await post('/auth/login',{username:u,password:p});nav('/')}catch(err){setE(err instanceof Error?err.message:'فشل الدخول')}finally{setBusy(false)}}return <div className="login-wrap"><form className="login-card" onSubmit={submit}><div className="login-icon">YT</div><h1>تسجيل الدخول</h1><p>التطبيق خاص بمدير النظام فقط</p><label>اسم المستخدم<input value={u} onChange={e=>setU(e.target.value)} autoComplete="username" required/></label><label>كلمة المرور<input type="password" value={p} onChange={e=>setP(e.target.value)} autoComplete="current-password" required/></label>{e&&<div className="alert error">{e}</div>}<button className="primary" disabled={busy}>{busy?'جارٍ الدخول...':'دخول'}</button></form></div>}
