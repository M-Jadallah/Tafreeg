function cookie(name:string){return document.cookie.split('; ').find(v=>v.startsWith(name+'='))?.split('=').slice(1).join('=')||''}
export class ApiError extends Error{status:number;detail:unknown;constructor(status:number,detail:unknown){super(typeof detail==='string'?detail:'حدث خطأ');this.status=status;this.detail=detail}}
export async function api<T>(path:string,options:RequestInit={}):Promise<T>{const method=(options.method||'GET').toUpperCase();const headers=new Headers(options.headers);if(options.body&&!(options.body instanceof FormData))headers.set('Content-Type','application/json');if(!['GET','HEAD','OPTIONS'].includes(method))headers.set('X-CSRF-Token',decodeURIComponent(cookie('csrf_token')));const response=await fetch('/api'+path,{...options,headers,credentials:'include'});if(response.status===401&&!path.includes('/auth/login')){window.location.href='/login';throw new ApiError(401,'انتهت الجلسة')}if(!response.ok){let detail:unknown=response.statusText;try{const body=await response.json();detail=body.detail??body}catch{}throw new ApiError(response.status,detail)}if(response.status===204)return undefined as T;return response.json()}
export const post=<T>(p:string,b?:unknown)=>api<T>(p,{method:'POST',body:b===undefined?undefined:JSON.stringify(b)});
export const put=<T>(p:string,b:unknown)=>api<T>(p,{method:'PUT',body:JSON.stringify(b)});
export const del=<T>(p:string,b?:unknown)=>api<T>(p,{method:'DELETE',body:b===undefined?undefined:JSON.stringify(b)});
export function formatBytes(n?:number){if(!n)return '0 ب';const u=['ب','ك.ب','م.ب','ج.ب','ت.ب'];const i=Math.min(Math.floor(Math.log(n)/Math.log(1024)),u.length-1);return `${(n/1024**i).toFixed(i?1:0)} ${u[i]}`}
export function formatDuration(s?:number){if(!s)return '—';const h=Math.floor(s/3600),m=Math.floor((s%3600)/60),x=Math.floor(s%60);return [h,m,x].filter((_,i)=>i>0||h>0).map(v=>String(v).padStart(2,'0')).join(':')}
